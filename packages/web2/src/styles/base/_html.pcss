html,
body,
#svelte {
  height: 100%;
  width: 100%;
}

html {
  font-family: var(--font-family-sans);
  font-size: 100%;
  color: var(--theme-color);
  background-color: var(--theme-background-color);

  @media (--large) {
    font-size: 110%;
  }
}
