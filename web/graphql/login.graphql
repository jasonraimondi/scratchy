mutation Login($data: LoginInput!) {
  login(data: $data) {
    accessToken
    user {
      id
      email
      name
    }
  }
}

mutation RefreshAccessToken {
  refreshAccessToken {
    accessToken
    user {
      id
      email
    }
  }
}
