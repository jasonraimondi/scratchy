mutation Login($data: LoginInput!) {
    login(data: $data) {
        accessToken
        user {
            id
            email
            name
        }
    }
}

mutation RefreshToken {
    refreshToken {
        accessToken
        user {
            email
        }
    }
}